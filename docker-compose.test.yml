networks:
  dev:
    external: true

volumes:
  frontend-assets-dev:
    external: true

services:
  postgres-pr:
    image: postgres:17
    container_name: postgres_container-pr
    env_file:
      - postgres.env
    networks:
      dev:


  backend-pr:
    image: andreyparkh/family-hub-backend-pr:latest
    container_name: family-hub-backend-pr
    ports:
      - "3001:3000"
    depends_on:
      - postgres-pr
    env_file:
      - backend.env
    networks:
      dev:


  frontend-pr:
    image: andreyparkh/my-calendar-frontend-pr:latest
    container_name: family-hub-frontend-pr
    ports:
      - "8081:8080"
    env_file:
      - frontend.env
    volumes:
      - frontend-assets-dev:/app/dist/client/assets/
    networks:
      dev:
